import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const SNotFound = styled(Box)(({ theme }) => ({
  background: "#1E1E1E",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "100vh",
}));
